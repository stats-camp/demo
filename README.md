# stats.camp Showcase Repo

This repository is the source material for the anonymous try-out link on the login page.

Open `showcase.do` to see the main demo. It imports `data/showcase.csv`, runs multiple regressions, stores estimates with `eststo`, renders an `esttab` comparison table, draws a `coefplot`, and produces additional plots.
